#ifndef DIFF_FUNC_H
#define DIFF_FUNC_H

// 函数声明
void diff_func(int i_tic, int i_lgkt, double* datain, double* Sheet1, double* Sheet2);

#endif // DIFF_FUNC_H
#pragma once




/*

// 在堆上分配内存
int* largeArray = new int[size];  // 动态分配

// 使用 largeArray 进行操作
for (int i = 0; i < size; ++i) {
    largeArray[i] = i;
}

// 不要忘记在使用完毕后释放内存
delete[] largeArray;  // 释放堆内存

*/